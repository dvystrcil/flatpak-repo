name: Test Flatpak Setup and Build

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: flatpack-runner
    steps:
      - uses: actions/checkout@v4

      - name: Install flatpak from PPA
        run: |
          sudo add-apt-repository -y ppa:flatpak/stable
          sudo apt-get update
          sudo apt-get install -y flatpak flatpak-builder
          flatpak --version
          flatpak-builder --version

      - name: Setup flatpak runtime
        run: |
          flatpak --user remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          flatpak --user install -y flathub org.freedesktop.Sdk/x86_64/23.08
          flatpak --user install -y flathub org.freedesktop.Platform/x86_64/23.08

      - name: Setup flatpak repo
        run: |
          mkdir -p flatpak-repo
          flatpak --user remote-add --no-gpg-verify --if-not-exists myrepo flatpak-repo

      - name: Test flatpak build
        run: |
          flatpak-builder --user --force-clean test-build com.megamek.mekhq.json || true

      - name: Verify build
        run: |
          ls -la test-build
